# 📊 Работа с базой гаплотипов (инструкция)

Этот проект нужен для обработки данных о гаплотипах из Excel-файлов:

1) данные сохраняются в базу,

2) автоматически разбираются на локусы и аллели,

3) по заданным правилам считаются итоговые таблицы,

4) результат сохраняется обратно в Excel.

Программа запускается через команды, без необходимости писать код.

# 🧩 Из чего состоит система

Есть три основных сущности:

1. Исследование (Research)

- номер исследования (например, 7 или 8)

- количество участвовавших людей


2. Образец / гаплотип (Sample)

- строка гаплотипа, пример:

```
A*01-B*08-DRB1*15-DQB1*06
```


- количество людей с таким гаплотипом

- ссылка на исследование


3. Локус (Locus)

- название локуса (A, B, DRB1 и т.д.)

- первый коэффициент

- второй коэффициент (если есть)

- исходная строка коэффициентов (как в Excel)


# 📁 Какие файлы используются

1️⃣ Excel с исследованиями

Содержит:

- номер исследования

- количество человек

Пример:

```
number	people_amounth

7	    120

8	    98
```


2️⃣ Excel с образцами (гаплотипами)

```
haplotype	                people_amounth	research

A*01-B*08-DRB1*15-DQB1*06	12	            7

A*02-B*07-DRB1*15-DQB1*06	8	            8
```


3️⃣ Excel с заданиями (что и как считать)

Один файл может содержать несколько задач.

Пример:

```
researches	full_loci	partial_loci	output

7,8	        A,B,C	    DRB1	        table_7_8.xlsx

7,8	        A,B		                    table_AB_only.xlsx
```


Пояснения:

researches — какие исследования сравнивать

full_loci — локусы, которые должны совпадать полностью

partial_loci — локусы, которые сравниваются только по первому коэффициенту

output — имя Excel-файла с результатом


# ▶️ Как пользоваться (по шагам)

Вся работа происходит в терминале!

Настройка программы:

Шаг 1. Склонируй проект с гитхаба:

```bash
git clone https://github.com/Gasuaska/haplotypes_research.git
```

Шаг 2. Настрой виртуальное окружение и установи зависимости:

Настройка виртуального окружения: 


Команда для Windows:

```bash
python -m venv venv
```

Команда для Linux и macOS:

```bash
python3 -m venv venv 
```

Команда для Windows:

```bash
source venv/Scripts/activate
```

Для Linux и macOS:

```bash
source venv/bin/activate 
```

Установка зависимостей:


```bash
pip install -r requirements.txt 
```

Готово! Проект может работать.

Обработка данных:

Шаг 1. Импорт исследований

```bash
python manage.py import_research research.xlsx
```


Что делает:

- читает Excel

- создаёт исследования в базе данных

Шаг 2. Импорт гаплотипов

```bash
python manage.py import_samples samples.xlsx
```


Что делает:

- читает гаплотипы

- сохраняет их в базе

- связывает с исследованиями

Шаг 3. Разбор гаплотипов на локусы

```bash
python manage.py proceed_samples
```


Что делает:

- разбивает строки вида

```
A*01-B*08-DRB1*15
```

- создаёт отдельные локусы

- аккуратно разбирает коэффициенты

- сложные случаи со / сохраняет как есть

Шаг 4. Подсчёт и выгрузка таблиц

```bash
python manage.py proceed_jobs job.xlsx
```


Что делает:

- читает Excel с заданиями

- для каждой строки:

1. выбирает нужные исследования

2. сравнивает гаплотипы по заданным правилам

- считает:

1. общее количество людей

2. отдельно по каждому исследованию

- сохраняет результат в Excel в папку results


# 📤 Что получается на выходе

Excel-файл вида:

```
Гаплотип	        Всего	Исследование 7	Исследование 8

A*01-B*08-DRB1*15	20	    12	            8

A*02-B*07-DRB1*15	9	    4	            5
```


⚠️ Важные замечания

- Названия локусов должны быть одинаковыми (A ≠ A␣)

- Коэффициенты лучше хранить как текст

- Если в коэффициентах есть /, они не участвуют в автоматическом сравнении
Excel можно менять и перезапускать команды сколько угодно раз (но лучше
переместить файлы результатов в другую папку)

# 💬 Если что-то пошло не так

Если программа ругается:

- проверь, нет ли пустых ячеек

- проверь, что номера исследований — целые числа

- убедись, что локусы написаны относительно корректно

# Для просмотра админки:

1. Создай суперпользователя:

```bash
python manage.py createsuperuser
```
Windows


```bash
python3 manage.py createsuperuser
```
для Mac и Linux

2. Запусти сервер:

```bash
python manage.py runserver
```

```bash
python3 manage.py runserver
```


Админка доступна по адресу 
```
127.0.0.1:8000/admin
```


Чтобы остановить сервер нажми Ctrl+C в терминале